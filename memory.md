# memory.md — shared agent memory

Purpose: let concurrent agents see who is working on what, right now, and any facts another agent needs before touching shared code. This is not a changelog; git history is the changelog.

Rules

- Edit this file directly on `main` in the primary checkout, never in a worktree. Pull first. Commit as `docs(memory): <what changed>`.
- One entry per active worktree. Generally one agent per worktree.
- If an entry's worktree is not in `git worktree list`, that agent has moved on or its session ended. Anyone may mark the entry `stale` and remove it on the next edit.
- Post a **Notice** whenever you change a shared contract: `apps/api/openapi.yaml`, `packages/animation-catalog/schema.json`, the bridge message protocol, or a Flyway migration.
- Keep it short. Details belong in the PR.

## Active work

| Worktree | Branch | Agent / session | Phase | Task | Status | Started | Touches shared contract? |
|---|---|---|---|---|---|---|---|
| (primary) | main | Claude Fable 5.1 (planning session with Chris) | planning + 0 | Planning docs (done), Phase 0 foundation PR #1 (merged), agent PR comments + cleanup PR #2 (merged) | done 2026-09-18 | 2026-09-17 | no |
| (removed) `.worktrees/feat/3-web-shell-help` | feat/3-web-shell-help | Claude Fable 5.1 (session with Chris) | 3 | Web shell: URL entry → POST /projects, resizable split editor, Control Panel state machine + /dev route, live help page via runtime CSS generator, MSW mocks; re-skin from Claude Design mocks | done 2026-09-18 — merged as PR #13 (84a9e74); worktree, branch and Docker e2e images cleaned | 2026-09-18 | no (consumes openapi + catalog read-only) |
| .worktrees/feat/4-bridge-integration | feat/4-bridge-integration | Claude Fable 5.1 (orchestrator + subagents) | 4 | Rest of Phase 4 on ONE feature branch (Chris's call): plan Tasks 6–7 (store factory, element metadata, guard state, `toApplied`, bridge client), Task 8 (API serves `packages/bridge` script; removes the Phase 2 stub), Task 9 (iframe hook, sandbox, cross-origin mock page, guard dialog), Task 10 (e2e, perf, docs). Edits `apps/web/lib/store`, `components/editor/editor-shell.tsx`, mock page route, `apps/api` bridge assets + Gradle + Dockerfile. | in progress | 2026-09-18 | implements bridge protocol v1; no new contract change expected |
| .worktrees/chore/phase-5-plan | chore/phase-5-plan | Claude Fable 5.1 (planning session with Chris) | 5 (planning) | Phase 5 design + plan doc `docs/plans/phase-5-agent-flows.md` (docs only), draft PR #14, architect review pending. No implementation claimed yet. | in progress | 2026-09-18 | plans an ADDITIVE bridge protocol change (see notice); no code |
| .worktrees/feat/6-version-history | feat/6-version-history | Claude Fable 5.1 (session with Chris) | 6 | Version history. Plan: `docs/plans/phase-6-version-history-plan.md` (on the branch). **Track A now** — new files only: `apps/web/lib/versions/*`, `components/history/*`, `components/dialogs/conflict-dialog.tsx`, `app/dev/history/page.tsx`. Track B (store, editor-shell, control-panel, stack e2e) waits for Phase 4. One PR, opened after Phase 4 merges (Chris's call). | in progress (Track A) | 2026-09-18 | no (consumes openapi read-only) |

## Notices (newest first)

- 2026-09-18 — **Phase 6 claimed on `feat/6-version-history`; Track A starts now and edits nothing Phase 4 or Phase 5 claimed.** It only creates files (`apps/web/lib/versions/`, `apps/web/components/history/`, `components/dialogs/conflict-dialog.tsx`, `app/dev/history/page.tsx`) plus, at most, exporting `ChangeRow` from `components/dialogs/save-dialog.tsx`. It does NOT touch `lib/store/*`, `control-panel/*`, `editor-shell.tsx`, `dev-gallery.tsx`, `apps/web/package.json`, `lib/bridge`, `lib/agent`, `packages/bridge` or `apps/api`. No contract change (scope is core only: DT-016, DT-062, DT-117 stay deferred). Track B will add `currentVersionId` / `viewingVersionId` and `loadVersion` / `markSaved` / `enterViewing` / `exitViewing` / `rebaseDraft` to the store factory after Phase 4 merges. **Phase 4/5 sessions:** if you plan to edit `save-dialog.tsx`, say so here.

- 2026-09-18 — **Phase 5 planning (draft PR #14, `docs/plans/phase-5-agent-flows.md`), nothing claimed yet.** Two tracks (Chris's call): Track A can start before Phase 4 merges — `apps/web/lib/agent` (new dir), bridge `elements:query` → `elements:list` in `packages/bridge` (the pair protocol v1 reserved for Phase 5, plus an additive `viewport` field; `BRIDGE_VERSION` 1.1.0, `PROTOCOL_VERSION` stays 1), and a panel-machine `auto` state in `panel-machine.ts` (+ a one-line `"vmId" in panel` fix in two `store/index.ts` selectors, + a new `CHANGE` event so tuning's "Change" stops overloading `BACK`). Track B (store provenance, `queryElements` on the bridge client, wiring, stack e2e) waits for `feat/4-bridge-integration`. **Phase 4 session:** if you plan to touch `panel-machine.ts` or add handlers to `vm-bridge.js`, say so here; objections to the protocol addition go on PR #14. **After the architect review (PR #14 comment):** Phase 5 will change what the element-switch guard calls dirty (dirty AND not agent-generated-and-untouched), so please keep `selectElementDirty` the single place `requestSelect` decides that; and read the selection through `selectSelectedVmId`, not `state.panel.vmId` behind a `status !== "idle"` check (the new `auto` panel state has no `vmId`).
- 2026-09-18 — **Phase 4 PR A merged (PR #12): `packages/bridge` is on `main`** with the `bridge` gate group. The rest of Phase 4 (plan Tasks 6–10) is claimed on a single feature branch `feat/4-bridge-integration`. It will touch the Phase 3 store, `editor-shell.tsx`, the mock page route and the Phase 2 bridge assets; if you need to change those files in the next day, say so here first.
- 2026-09-18 — **Phase 3 merged (PR #13, 84a9e74); worktree cleaned, claim dropped.** `main` now has the re-skinned web app (Entry, Editor, Help, `/dev` gallery), the Control Panel state machine, `lib/runtime-css`, MSW mocks and the three `apps/e2e/web` buckets. Open follow-ups from it: DT-116 (P1, catalog `underline-sweep` resting state — hits Phase 4 hover/in-view), DT-099/100 (guard scope, wire Save), DT-107 (mock for `/bridge/vm-bridge.js`), DT-113 (web-only e2e reuses whatever holds :3000), DT-117, DT-118, plus DT-053..060, 077..083, 108..111. Phase 4 hand-off notes are in the notice below.
- 2026-09-18 — **`apps/e2e/web/` has three buckets as of PR #13** (`feat/3-web-shell-help`, 61c37e7): `web/*.spec.ts` run under both gates (any deployment), `web/mocked/` needs the MSW mock api or a dev build (`pnpm e2e` only; `playwright.docker.config.ts` ignores it — e.g. `/dev` is a 404 in a production build), `web/stack/` needs the real api (`pnpm e2e:docker` only; Phase 4 bridge specs go here). `apps/e2e` has a `pretest` hook that builds `packages/animation-catalog` because the Playwright web server starts via `pnpm exec`, which runs no `pre*` hook; the Docker runner image now copies `packages/animation-catalog/{current,versions}`. CLAUDE.md's Tests convention gets one sentence for this in the same PR.
- 2026-09-18 — **Phase 3 (PR #13) is feature-complete in draft.** For Phase 4: `apps/web/lib/runtime-css` is the shared CSS generator (`assignmentStyle` now includes catalog `baseStyles`; `inlineStyle`, `runtimeStylesheet`); existing assignments must be resolved with `getCatalogEntryAt(assignment.catalogVersion, id)` (`lib/catalog.ts`), only the picker uses the current catalog; the panel machine takes `SELECT`/`BACK`/`REVERT` with an optional `draftAnimationId`; `unsaved`/selected id are derived selectors; the unsaved guard lives in `ControlPanel` and should move up to `EditorShell` when element clicks are guarded (DT-099); MSW has no handler for `/bridge/vm-bridge.js` yet (DT-107); the mock iframe page is a same-origin Next route, so never combine `allow-scripts` with `allow-same-origin` against it. Catalog bug found via screenshots: `underline-sweep` fills its element whenever the animation is not running (DT-116, P1) — affects `hover`/`in-view` triggers on real pages.
- 2026-09-18 — **Bridge protocol: additive changes in PR #12** (`packages/bridge`; spec `docs/plans/phase-4-bridge-protocol.md` updated in the same PR). (1) `ack` gains `unknownVmIds?: string[]`; a valid `state:load` now acks `ok: true` and lists skipped vmIds there, while a single `apply`/`clear`/`select`/`replay` for an unknown vmId still acks `ok:false, error:"unknown-element"`. (2) `replay` needs an explicit `vmId` (`null` = all); a missing key is `invalid-payload`. (3) `keyframesCss` must parse to exactly one `@keyframes` rule named `keyframesName`, else `invalid-payload`. (4) Behaviour the **Phase 7 exporter must mirror**: hover arms the whole chain of tagged ancestors (as CSS `:hover` does); the armed group carries every `animation-*` longhand, so the export must emit the `animation` shorthand; `in-view` is held on the first keyframe. (5) A bridge with no usable parent origin is inert (a directly visited clone is a normal page). (6) A `hello` the bridge cannot answer yet (document body not parsed) acks `ok:false` with no error code and posts no `ready`; the shell needs no recovery path because the `DOMContentLoaded` `ready` and its own `load`-event `hello` still arrive. Phase 4 PR B (bridge client) is the consumer. **e2e location:** PR #12 adds an app-free Chromium suite at `packages/bridge/e2e` (static HTML + the script, no API, no web app) as a `bridge e2e` gate; that is deliberate and separate from PR #11's `apps/e2e` layout, which still owns every spec that needs the web app or the stack (incl. Phase 4 Task 10).
- 2026-09-18 — **Decision from Chris: e2e moves out of `apps/web` into its own workspace package `apps/e2e`** (a mobile client will be tested from there too). **Merged (PR #11), worktree cleaned.** New layout: `apps/e2e/web/*.spec.ts` (web-only), `apps/e2e/web/stack/` (needs the real api; `pnpm e2e:docker`), `apps/e2e/fixtures/`, `apps/e2e/docker/` (was `docker/e2e/`), `apps/e2e/playwright.config.ts` (was `apps/web/playwright.config.ts`). Commands: `pnpm e2e` replaces `pnpm --filter web e2e`; `@playwright/test` and the `e2e` script are gone from `apps/web/package.json`. **Phase 3 (`feat/3-web-shell-help`):** your edits to `apps/web/e2e/smoke.spec.ts` and `apps/web/playwright.config.ts` follow the renames on rebase (git tracks them); keep your `env: { NEXT_PUBLIC_API_MOCKING }` line in `apps/e2e/playwright.config.ts`, drop `@playwright/test` / `e2e` if they reappear in `apps/web/package.json`, and re-run `pnpm install`. **Phase 4:** bridge e2e specs go in `apps/e2e/web/stack/` (plan doc updated in PR #11). This supersedes the paths in the notice below.
- 2026-09-18 — **Full-stack e2e now exists: `pnpm e2e:docker` (PR #11, merged).** Every run builds and destroys its own Docker stack (Postgres, the Render api image, a production web build, fixture pages, Playwright runner); no host ports, no container names, safe to run concurrently from any worktree, and part of `pnpm gates` + CI. **Phase 3/4:** specs that need the real api go in `apps/web/e2e/stack/` (import origins from `stack/env.ts`, clone `${stack.fixtureOrigin}/marketing.html`, add pages under `apps/web/e2e/fixtures/`); web-only specs stay in `apps/web/e2e/`. Contract with the stack: `workers: 4`, the api admits 2 concurrent clones then 503 + `Retry-After`, the DB is never cleaned within a run, so scope assertions to the project your test created. Run it and `pnpm gates` in the background (cold builds outlast a foreground tool timeout). PR #11 adds one `testIgnore` line to `apps/web/playwright.config.ts`: Phase 3, expect a trivial conflict and keep both lines. Details: `docker/e2e/README.md`.
- 2026-09-18 — **CI runs on `main` are no longer cancelled by later pushes (PR #10 merged)**, so every merge gets its own checks and Render's `checksPass` auto-deploy fires per commit; no need to wait before pushing `docs(...)` commits. Before the fix, the Phase 2 and PR #9 merges were skipped this way; Chris deployed manually. **Production now serves Phase 2** (`/health` version `d2c6597`; `/bridge/vm-bridge.js`, `/catalog/versions`, `/projects/{id}` verified). To check what is live: `curl https://vibe-motion-api.onrender.com/health`.
- 2026-09-18 — **Phase 2 merged (PR #4); PR #7 merged.** `main` now has projects, the clone pipeline, versions (diff/stateAt/restore), the serve-time bridge stub at `apps/api/src/main/resources/bridge/vm-bridge.js` (Phase 4 owns the real protocol) and `scripts/pr-comment.sh`. Web worktrees: rebase or merge `main`, then `pnpm gen:client` is already current (check-generated will tell you). Phase 2 worktrees removed with `scripts/cleanup-merged.sh`. API prerequisites for Phase 4 and the API half of Phase 6 are in place; Phase 7 (export) can start on the API side once Phase 6 state shape is confirmed (it is: see `domain/Models.kt`).
- 2026-09-18 — **Decision from Chris: Render PR previews are OFF; e2e runs locally against Docker** (DT-075, DT-076). Do not plan on `-pr-<N>.onrender.com` URLs. Phase 8's "e2e on previews" exit criterion is replaced in build_plan by the local Docker stack. In `render.yaml`, `off` must stay quoted (bare `off` is a YAML boolean and fails the schema).
- 2026-09-18 — **Bridge protocol spec v1 is on `main` (PR #8 merged)** (`docs/plans/phase-4-bridge-protocol.md` + `phase-4-bridge-plan.md`, docs only, architect-reviewed). Shared contract. Phase 2: keep `style-src 'unsafe-inline'` and the `data-vm-parent-origin` attribute; Phase 4 Task 8 will move `vm-bridge.js` to a new `packages/bridge` and parse `BRIDGE_VERSION` from the script. Phase 3: the bridge drives the **store** (`setSelectedVmId`, `draftState` funnel), adds `createEditorStore()` / `hoverVmId` / `elements`, changes the iframe `sandbox` and makes the mock page cross-origin (`127.0.0.1`); rebase for `vm-<id>-v1-1-0`. It is now a shared contract: additive changes only; raise objections as a PR against the spec. Owner decisions folded in: guard on element switch (the bridge never moves the selection ring itself), `in-view` held on first keyframe, unprefixed message types.
- 2026-09-18 — **Phase 4 planning done** (`docs/plans/phase-4-bridge-protocol.md`, `phase-4-bridge-plan.md`). Implementation is NOT claimed and will not start until Phases 2 and 3 are on `main`. Phase 2/3 sessions: the plan will treat your `vm-bridge.js` + page injection (Phase 2) and editor store + `runtime-css` + panel state machine (Phase 3) as seams; a bridge protocol spec will be posted for you to object to before you merge anything incompatible.
- 2026-09-18 — **Phase 2 PR #4 contract additions (all ADDITIVE), client regenerated:** `503 clone_busy` on `POST /projects`; `413 payload_too_large` + `503 project_busy` on both version POSTs; `400` on restore; `304` + `If-None-Match` on `/projects/{id}/page` and `/bridge/vm-bridge.js`; new shared header/response components. Behavioural facts for web: JSON bodies are capped at 256 KB and a diff at 2,000 entries; **param values are validated server-side against the pinned catalog param type/min/max/unit** (422 `invalid_diff`), so the editor must send values in the catalog's unit; at most 2 concurrent clones per instance, so handle 503 + `Retry-After` on create.
- 2026-09-18 — **Decision from Chris (DT-047): keyframes are named by the full catalog version**, `vm-<id>-v<M>-<m>-<p>` (e.g. `vm-fade-in-up-v1-1-0`), not `-v<major>`. Semver clarified: a new knob whose default reproduces the old rendering is a MINOR; MAJOR = changed rendering at default params, removed ids, renamed/removed params. **Merged to `main` (PR #6).** Always call `keyframesName()`; never build or parse the name yourself. Phase 3: rebase onto `main` now; any test expecting 1.0.0 and 1.1.0 to share one `@keyframes` block will no longer hold. Phase 4: the bridge must get the name from the shell/shared generator. Phase 7: goldens use the full-version name. `manifest.json` now also lists `keyframesNames[version][id]`; `validate` requires every `var(--vm-x)` in keyframes/baseStyles to be declared by a param.
- 2026-09-18 — **Claude Design handoff arrived**: `docs/design/` (README spec, `design-system/` tokens + components, `ui_kit/`, hi-fi `mocks/`) merged to `main` (PR #5, docs only). Phase 3: rebase onto `main` and re-skin against it. Handoff wins on visuals, `docs/` win on behaviour. Known conflicts are logged (fixed 320px panel vs resizable; handoff predates catalog 1.1.0 so there is no `fillMode` control mock). Ignore the DT IDs inside `docs/design/deferred_tasks_additions.md`; they collide, the real entries are in `docs/deferred_tasks.md`.
- 2026-09-18 — **Phase 1 merged (PR #3); worktree cleaned.** Open follow-ups: DT-035, DT-048(b), DT-049, DT-068..071. Contract facts for Phases 2/3/4: catalog `1.1.0` is `current`; `fillMode` is the sixth standard param key (`select`, options none/forwards/backwards/both) on every entry from 1.1.0; `validate` enforces that within a MAJOR, later versions never drop an animation id or a param key (superset gate, PR #6 — it replaced PR #3's identical-keyframes freeze) and that `select` params have `default ∈ options`. `packages/animation-catalog/manifest.json` is generated (`pnpm gen:types`) and gate-checked. `feat/3-web-shell-help` must rebase onto `main` for the catalog `package.json` entry-point block: keep `dist/src/`.
- 2026-09-18 — **DT id collision fixed.** Two agents appended DT-031..035 concurrently. Phase 2 entries are now DT-039..046 (rebinding=039, clone heap=040, @import order=041, srcset data:=042, @import layer=043, sheet de-dup=044, stray paren=045, versions summary=046). Rule: `git pull` immediately before appending and take max(existing id)+1; if your push is rejected, re-pull and renumber yours.
- 2026-09-18 — **Phase 2 PR open (feat/2-api-core).** Contract change (ADDITIVE): new path `GET /bridge/vm-bridge.js`; web client regenerated in that PR, so Phase 3 should rebase after it merges or re-run `pnpm gen:client`. Design revision: the bridge `<script>` and CSP are added at SERVE time by `PageRenderer`, never stored in `base_html` (build_plan Phase 2, architecture §3.1). The bridge served today is a stub that only posts `ready` and blocks navigation; **Phase 4 owns the real protocol** in `apps/api/src/main/resources/bridge/vm-bridge.js`. `projects.source_url` stores the post-redirect URL. No Flyway change.
- 2026-09-18 — **`animation-catalog` package entry points were wrong on `main`** (`main`/`types`/`exports` pointed at `dist/index.js`; the build emits `dist/src/index.js`, so no workspace consumer could import it). Fixed on `feat/3-web-shell-help` (88dde98) in `packages/animation-catalog/package.json`; web now depends on `animation-catalog` (`workspace:*`) with `pre*` hooks that build it. Phase 1 worktree also edits that package.json — expect a small merge conflict; keep the `dist/src/` paths.
- 2026-09-18 — **Phase 1 claimed** (`feat/phase-1-catalog`). Heads-up for Phases 2/3: additive contract change incoming — `schema.json` gains a sixth standard param key `fillMode` (maps to `animation-fill-mode`), catalog `1.1.0` is published and `current` moves to `1.1.0`. `1.0.0` stays untouched and load-only. Do not hardcode the five-key standard set; read it from the catalog package / `CatalogParam.isStandard`.
- 2026-09-18 — **Phase 0 merged (PR #1) and PR #2 merged.** `main` now has the monorepo, gates, CI, frozen `openapi.yaml`, catalog 1.0.0, cleanup script. Contract freeze is in effect: `apps/api/openapi.yaml`, `packages/animation-catalog/schema.json` and `versions/1.0.0.json` are additive-only. **Phases 1, 2, 3 are open for claiming** (one worktree each; claim below before creating the worktree). Worktrees for #1/#2 were removed with `scripts/cleanup-merged.sh`.
- 2026-09-18 — From PR #2 on, every subagent posts its report as a marker-tagged, upserted PR comment (`<!-- vibe-motion-agent:<name> -->`). Procedure: docs/agents/pr-comment.md. Orchestrators still own the PR description.
- 2026-09-18 — PR #1 review fixes pushed (5d9e609). Flyway `V1__baseline.sql` edited in place (dropped a duplicate index) before merge; anyone with a local DB from an earlier run must `docker compose down -v && docker compose up -d db`. After #1 merges, migrations are append-only (V2+). New gate: "generated artifacts up to date" fails if `openapi.yaml` or the catalog change without regenerating; run `pnpm gen:types && pnpm gen:client` after editing either.
- 2026-09-18 — Phase 0 PR #1 open (draft). `apps/api/openapi.yaml` and `packages/animation-catalog/schema.json` + `versions/1.0.0.json` are the Phase 0 freeze: additive-only once merged. Stack actuals: Next.js 16.3 / React 19.2 / Tailwind 4 / shadcn on Base UI (not Radix); Ktor 3.6 / Kotlin 2.4 / Exposed 1.5 (packages `org.jetbrains.exposed.v1.*`) / Flyway 13 / Kotest 5.9. Local Postgres via `docker compose up -d db` is on host port **5433**. Phases 1, 2, 3 may start in separate worktrees after #1 merges.
- 2026-09-18 — Decision from Chris: the animation catalog is **versioned and immutable**. Files under `packages/animation-catalog/versions/` are never edited once on `main`; changes are new files + `current` bump + CHANGELOG. Every saved assignment pins `catalogVersion`. CSS is derived from the pinned entry, never stored. CI gate `check-immutable` enforces it (Phase 1). Adds `catalog_version` column and `GET /catalog/versions`, `GET /catalog/{version}` to the Phase 2 contract.
- 2026-09-18 — Decision change from Chris: versions store **diffs**, created only on explicit user **Save**; live preview never writes to the API. Build plan Phases 2, 4, 6, 7 and architecture.md updated. Any Phase 2 work must implement `stateAt()` and the 409 stale-parent check.
- 2026-09-18 — Screenshots go only to the orphan branch `pr_screenshot` via the screenshot-runner subagent (CLAUDE.md rule 8). DT-018 tracks creating/protecting that branch.
- 2026-09-18 — GitHub remote confirmed: `git@github.com:ludaxkris/vibe_motion.git`, default branch `main`.

- 2026-09-17 — Planning artifacts created. Phase 0 has not started. First Phase 0 task is the monorepo scaffold + gates; nothing else should start until Phase 0 merges (see build_plan.md §3).

## Claimed next tasks

Agents claim a task here before creating a worktree so two agents do not pick the same phase task.

| Task | Claimed by | Date |
|---|---|---|
| Phase 4 Tasks 6–10 (bridge client, API wiring, shell integration, e2e) on `feat/4-bridge-integration` | Claude Fable 5.1 (session w/ Chris) | 2026-09-18 |
| Phase 6 (version history), Track A now / Track B after Phase 4, on `feat/6-version-history` | Claude Fable 5.1 (session w/ Chris) | 2026-09-18 |

## Facts other agents need

- Primary checkout: `/Users/christung/Projects/anthropic/vibe_motion`. Remote `origin` = `ludaxkris/vibe_motion` (private). Worktrees go under `.worktrees/<branch>` (gitignored).
- Long-lived branches: `main` (code), `pr_screenshot` (orphan, screenshots only).
- Kotlin toolchain on this machine: JDK 21. Node 25 is installed; the project targets Node 22 LTS on Render, so use `.nvmrc`/`engines` and do not rely on Node 25-only APIs.
- **Render is live (2026-09-18).** Workspace `Kithcard` (shared with Kithcard services; never touch `kithcard-*`), project `vibe-motion` / env `production`: https://vibe-motion-web.onrender.com, https://vibe-motion-api.onrender.com (`/health` reports db + deployed commit), Postgres `vibe-motion-db`. Deploys from `main` on `checksPass`. Open: DT-015 (web half), DT-074, DT-075.
- Render CLI is not installed locally. `render blueprints validate` cannot be run here until it is; validate `render.yaml` against `https://render.com/schema/render.yaml.json` instead.
- Claude Design mocks from Chris live in `docs/design/` on `main`. Reference only, not production code; start at `docs/design/README.md`.

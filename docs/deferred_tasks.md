# Deferred tasks and bugs

Append-only log. Edited directly on `main` in the primary checkout (see CLAUDE.md, "Shared coordination files"). This will migrate to Linear; keep titles issue-ready.

Priority: P0 blocks launch or corrupts data · P1 user-visible defect in a core flow · P2 degraded experience or tech debt with known cost · P3 nice to have.
Urgency: `now` · `this-phase` · `later`.
Status: `open` · `in-progress` · `done` · `wont-do`.

## Deferred tasks

| ID | Type | Title | Priority | Urgency | Phase | Owner | Status | Notes |
|---|---|---|---|---|---|---|---|---|
| DT-001 | feature | Figma file ingestion → functioning web page | P2 | later | post-v0 | — | open | Plugs in as a source adapter in the clone service (architecture.md §7). |
| DT-002 | feature | PNG / image ingestion → functioning web page | P2 | later | post-v0 | — | open | Same adapter seam as DT-001. |
| DT-003 | feature | Real LLM-backed animation agent replacing MockAnimationAgent | P1 | later | post-v0 | — | open | Implement `AnimationAgent` server-side; UI already passes the prompt text. |
| DT-004 | feature | Headless-browser clone mode for JS-rendered pages | P2 | later | post-v0 | — | open | Playwright/Chromium on Render is heavy; evaluate a separate worker service. |
| DT-005 | feature | Opt-in "keep source scripts" clone mode | P3 | later | post-v0 | — | open | v0 strips all `<script>` tags for stability. |
| DT-006 | feature | Editable version labels | P3 | later | 6 | — | open | v0 labels are auto-generated only. |
| DT-007 | feature | Auth, ownership, and sharing of projects | P1 | later | post-v0 | — | open | `owner_id` on projects; project URLs are unguessable UUIDs in v0 but not private. |
| DT-008 | feature | JS / scroll-driven / Web Animations API animations | P2 | later | post-v0 | — | open | Needs `engine` field on catalog entries and a JS emitter in the exporter. |
| DT-009 | feature | Store cloned binary assets (images, fonts) instead of absolutizing URLs | P2 | later | post-v0 | — | open | Requires object storage; v0 keeps one HTML document per project in Postgres. |
| DT-010 | feature | Multi-page projects | P3 | later | post-v0 | — | open | v0: one URL = one project. |
| DT-011 | chore | Migrate this log to Linear | P3 | later | — | Chris | open | Blocked on tooling availability. |
| DT-012 | feature | Persist Control Panel width per browser | P3 | this-phase | 3 | — | open | Open question in build_plan.md §7. |
| DT-013 | feature | Rate limiting on `POST /projects` | P1 | this-phase | 8 | — | open | Clone is the only expensive endpoint. |
| DT-014 | chore | Screenshot-Runner: define exact deliverable | P3 | now | 0 | Chris | open | Role description in the brief was truncated ("Take ..."). Assumed: capture screenshots of running app states for PRs and mock comparison. Confirm. |
| DT-015 | chore | Verify `fromService.envVarKey: RENDER_EXTERNAL_URL` resolves cross-service URLs in render.yaml (web ↔ api) on first Render deploy | P1 | this-phase | 0 | — | open (api half verified) | 2026-09-18 first Blueprint sync: `WEB_ORIGIN` resolves correctly in production AND in PR previews (preview api allows only its own `-pr-N` web origin; CORS probed). `NEXT_PUBLIC_API_ORIGIN` on web is NOT yet verifiable: nothing on `main` imports the api client, so the value is in no served chunk. Re-check the bundle once Phase 3 merges, then close. Original note: Render docs confirm envVarKey copies a var from another service; unconfirmed for platform-provided vars. Fallback: `sync: false` and set manually. Affects previews most. |
| DT-016 | feature | Persist unsaved draft state to localStorage as a crash/tab-close safety net | P2 | this-phase | 6 | — | open | v0 has an unsaved indicator and beforeunload warning only. |
| DT-017 | perf | Checkpoint snapshots for version history so `stateAt(N)` does not replay every diff on long histories | P3 | later | post-v0 | — | open | Trigger: a project exceeding ~200 versions. Fold cost is negligible at v0 scale. |
| DT-018 | chore | Protect the `pr_screenshot` branch from deletion and force-push | P2 | this-phase | 0 | Chris | in-progress | Branch created and pushed 2026-09-18 with a README. Remaining: add a branch protection or ruleset in GitHub settings (needs repo admin; may require a paid plan on private repos). |
| DT-019 | feature | "Upgrade to latest catalog" action for saved assignments pinned to an older catalog version | P3 | later | post-v0 | — | open | Explicit user action producing a new version whose diff rewrites `catalogVersion` and remaps params. v0 never auto-upgrades. |
| DT-020 | tech-debt | `apps/web/lib/catalog.ts` hardcodes `CURRENT_CATALOG_VERSION` and the 1.0.0 import path; must be bumped by hand on a catalog release | P2 | this-phase | 3 | — | open | Phase 3 should import from `animation-catalog` (CATALOGS/CURRENT_VERSION) or read `GET /catalog`. The web test now reads `packages/animation-catalog/current`, so a pointer bump without a web update fails the web unit gate. Still should import from `animation-catalog` in Phase 3. |
| DT-021 | feature | MSW handlers implementing openapi.yaml so the web shell is clickable without the API | P2 | this-phase | 3 | — | open | Planned in build_plan Phase 3. |
| DT-022 | feature | Draggable Control Panel resizer clamped 20–30% | P3 | this-phase | 3 | — | open | Phase 0 uses CSS `clamp(20%, 25%, 30%)`. |
| DT-023 | chore | Upgrade Kotest 5.9 → 6.x | P3 | later | — | — | open | Brief pinned 5.x; 6.2.5 is current stable. |
| DT-024 | perf | API Docker image is 547 MB; evaluate jlink or alpine JRE base | P3 | later | 8 | — | open | Starter plan pulls this on every deploy. |
| DT-025 | chore | Root `pnpm-workspace.yaml` `onlyBuiltDependencies` list needs revisiting when deps change | P3 | later | — | — | open | Added esbuild, unrs-resolver, sharp to silence pnpm 10 build-script prompts. |
| DT-026 | tech-debt | Web Zustand store is a module-scope singleton, shared across requests during SSR | P2 | this-phase | 4 | — | open | Harmless while nothing writes during render; Phase 4/5 populate per-user draft state. Use a context-based store factory like the QueryClient in providers.tsx. |
| DT-027 | test | Playwright e2e runs against `next dev`, not the production build Render serves | P3 | later | 8 | — | done | Add a CI variant running against `next build && next start`. Done in PR #11: the Docker stack runs `next build` + `next start` with render.yaml's commands. |
| DT-028 | chore | Add a Flyway V2 discipline note: V1 baseline was edited in place before merge; local DBs migrated earlier need `docker compose down -v` | P3 | now | 0 | — | done | One-time; after PR #1 merges, migrations are append-only. |
| DT-029 | bug-risk | `requireWebOrigin` returns WEB_ORIGIN verbatim; a mixed-case host may not match the lowercased browser `Origin` in CORS | P3 | later | 2 | — | open | Normalise scheme + host to lowercase and drop the path. Low confidence Ktor does not already normalise. |
| DT-030 | chore | `scripts/check-generated.mjs` header says it leaves the tree untouched; on failure it intentionally leaves regenerated files in place | P3 | later | — | — | open | Two-word comment fix; also re-add `**/coverage`, `**/test-results`, `**/playwright-report` to root .dockerignore. |
| DT-031 | test | MSW mock `DELETE /projects/{projectId}` handler has no unit test | P3 | later | 3 | — | open | Two-line handler used only for e2e cleanup (apps/web/mocks). |
| DT-032 | tech-debt | MSW mock 422 validation checks catalogVersion/animationId/param keys but not param values | P3 | later | 3 | — | open | Real API validates more; tighten if UI work starts relying on the mock to catch malformed params. |
| DT-033 | tech-debt | MSW mock `/export` output is not golden-matched against the Kotlin exporter | P3 | later | 7 | — | open | Do not assume byte-identical mock/real export CSS; align when Phase 7 lands. |
| DT-034 | tech-debt | `catalogResources` Gradle task writes `versions.txt` in lexical, not semver, order | P3 | later | 1 | — | open | `apps/api/build.gradle.kts` uses `.sorted()`. Harmless today because `ClasspathCatalogRepository` re-sorts with `SEMVER_ORDER`; would misorder `1.10.0` vs `1.9.0` for anything reading the file order directly. Found in PR #3 review. |
| DT-035 | feature | Catalog: back-fill `iteration` and `direction` standard params onto every entry (new minor catalog version) | P2 | this-phase | 1 | — | open | build_plan §4 Phase 1 "Common params" says every animation exposes duration, delay, easing, iteration, direction, fill-mode. Catalog 1.1.0 (PR #3) added `fillMode` everywhere but left 12 of 26 entries without `iteration` and all but `spin` without `direction`. Deliberate deviation: one-shot entrance/hover entries gain little. Needs Chris's call: ship a 1.2.0 that back-fills, or amend the plan. Raised in PR #3 description. |
| DT-039 | security | DNS-rebinding TOCTOU: the JDK HTTP client re-resolves the host at connect time, after the SSRF guard vetted it | P2 | this-phase | 8 | — | open | Fix needs a connect path pinned to the vetted InetAddress (custom connector) or an egress proxy with the same rules. Documented in SsrfGuard KDoc. Mitigated today by guard-per-hop and no proxy. |
| DT-040 | perf | Clone heap cost on the Render starter plan: one 10 MB page costs ~30 MB+ transient heap; concurrent POST /projects multiply it | P2 | this-phase | 8 | — | done | Add a concurrency limit (semaphore) on clone alongside the DT-013 rate limit, or lower CLONE_MAX_BYTES. |
| DT-041 | bug | Inlining an `@import` can push later un-fetched `@import` rules out of legal position so browsers drop them | P3 | later | — | — | open | Hoist remaining @import rules to the top of the inlined block. |
| DT-042 | bug | `srcset` containing a `data:` URI is left unrewritten, so relative candidates beside it stay relative | P3 | later | — | — | open | Comma-splitting is unsafe with base64 payloads. |
| DT-043 | feature | Modern `@import layer(...)` / `supports(...)` syntax is neither inlined nor absolutised | P3 | later | — | — | open | |
| DT-044 | perf | Stylesheets are not de-duplicated within a clone; the same sheet linked twice is fetched twice and spends budget twice | P3 | later | — | — | open | |
| DT-045 | polish | A neutralised `url(javascript:...)` leaves a stray `)`; the declaration is dropped by browsers but looks odd in exports | P3 | later | 7 | — | done | Revisit when the exporter lands. |
| DT-046 | perf | `GET /projects/{id}/versions` returns the full diff of every version; add a summary projection for long histories | P3 | later | 6 | — | open | Sibling of DT-017. |
| DT-047 | tech-debt | `keyframesName()` keys on catalog MAJOR only, so a MINOR can never add a cssVar param to an existing entry | P2 | this-phase | 1 | — | done | Fixed in PR #6: keyframes named by full catalog version `vm-<id>-v<M>-<m>-<p>`; same-major byte-identity freeze removed and replaced by an id/param-key superset gate. Decision by Chris 2026-09-18. Follow-ups: DT-068, DT-070. |
| DT-048 | tech-debt | Catalog/generated gates have two coverage gaps | P2 | this-phase | 1 | — | open | **(a) done in PR #6** (superset gate: ids and param keys never dropped within a major). Remaining: (b) only. (a) `validate.mjs` same-major pass does not require `prior ids ⊆ later ids`, so a MINOR that drops an animation passes every gate. (b) `scripts/check-generated.mjs` silently skips a `generated[]` path that is missing both before and after regeneration (e.g. after a rename) and still prints OK. From PR #3 re-review (NEW-2, NEW-3). |
| DT-049 | chore | PR #3 re-review nits in catalog tests/docs | P3 | later | 1 | — | open | NEW-4..11 in the code-reviewer comment on PR #3: duplicated `compareSemver` helpers (Kotlin test depends on a top-level `internal fun` in a sibling test file), stale `catalogResources` mentions in `CatalogManifestTest.kt`, CHANGELOG wording contradictions, near-tautological ordering assertion, no existence check in `catalogManifestResources`, README not updated for the select/same-major rules, vacuous no-label test, duplicated git fixture in `validate.test.ts`. |
| DT-050 | feature | Resizable Control Panel (drag handle, clamp 20–30%) | P3 | later | 3 | — | done | Design handoff (`docs/design/`, PR #5) ships the editor with a fixed 320px panel and drops resize from v0; build_plan §4 Phase 3 says resizable, clamped 20–30%. Superseded 2026-09-18 (Chris: docs win on behaviour, handoff on visuals): resize is behaviour, so Phase 3 keeps the resizable split (clamp 20–30%, persisted) and renders the handoff's 320px at the 25% default with the separator drawn as the panel's 1px border. Entry kept to record the conflict. The handoff's own `deferred_tasks_additions.md` calls this DT-020, which collides with the existing DT-020; this is the real ID. |
| DT-051 | feature | Dark editor theme | P3 | later | 3 | — | open | Dropped from v0 in the design review. Cloned page keeps its own colours regardless. (Handoff file calls this DT-021; that ID is taken.) |
| DT-052 | chore | Design handoff is stale against `main` in four places | P2 | this-phase | 3 | — | open | Also: handoff files show major-only keyframes names (`vm-fade-in-up-v1` in `ui_kit/Editor.jsx` Export snippet, both mocks, `design-system/readme.md`, `guidelines/type-mono.html`); real names are `vm-<id>-v<M>-<m>-<p>` per PR #6 — Phase 3 Export tab must use `keyframesName()`, not the mock text. `docs/design/README.md` says Next.js 15 (repo is 16), help page chip "catalog 1.0.0" / 12 animations (current is 1.1.0 with 26), tuning panel has no `fillMode` or `direction` control (catalog 1.1.0 exposes `fillMode` on every entry), and Assignment params omit `fillMode`. Per the handoff's own rule docs win on behaviour: Phase 3 renders controls from catalog params, so `fillMode` needs a control (suggest a dense segmented like Repeat). Ask Chris/Design for the missing control mock. |
| DT-053 | conflict | Entry: handoff shows a "Cloned" card + "Open in editor →" step; docs redirect straight to the editor | P3 | later | 3 | — | open | `docs/user_flow.md` §1: clone ok → `/p/{id}`. Docs win on behaviour; the card is not built. Revisit with Design if the confirmation step is wanted. |
| DT-054 | feature | Clone progress events (fetch / inline / sanitise / inject) so the Entry cloning card can show real step progress | P2 | later | 8 | — | open | Handoff 3a shows four ticking steps and a 62% bar; the API is a single `POST /projects`. Phase 3 shows the steps as pending with an indeterminate bar rather than faking progress. Needs an additive contract change (SSE or polling). |
| DT-055 | feature | `GET /projects` list endpoint for Entry "Recent projects" (+ "View all") | P2 | later | 8 | — | open | Handoff shows server-style recents; no list endpoint in `openapi.yaml`. Phase 3 keeps the last 8 in `localStorage` (`vm-recent-projects`), per browser only; no thumbnail, no "View all". |
| DT-056 | polish | Entry: specific error copy for 413 (too large) and 429 (rate limited) was generic in the wireframe pass | P3 | this-phase | 3 | — | done | Folded into the Phase 3 Entry re-skin (plan Task 7); Landed on `feat/3-web-shell-help` (Entry keys on the API's own codes: `page_too_large`, 429, `clone_busy`…). |
| DT-057 | polish | Editor load error does not distinguish network failure from API 5xx | P3 | later | 3 | — | open | `components/editor/editor-shell.tsx` shows one generic message + Retry. |
| DT-058 | polish | Panel width does not sync across tabs | P3 | later | 3 | — | open | `vm-panel-width` is read once per mount; add a `storage` listener if it matters. |
| DT-059 | tech-debt | Easing / direction / iteration option lists are hard-coded in the web tuning panel when the catalog param has no `options` | P2 | later | 1 | — | open | CLAUDE.md: add a params entry rather than special-casing in code. Needs a new catalog minor that declares `options` for these param types; then delete the fallbacks in `components/control-panel/tuning.tsx`. |
| DT-060 | tech-debt | Web has two structurally different `CatalogEntry` types (OpenAPI-generated vs `animation-catalog` package) bridged by one cast in `lib/catalog.ts` | P3 | later | 4 | — | open | Pick one source of truth for web (the package) once the bridge lands. |
| DT-061 | perf | `GET /projects/{id}/page` copies base_html several times per view (~100 MB transient for a 10 MB page); stream it instead of String + StringBuilder | P2 | this-phase | 4 | — | open | Not behind the clone semaphore. 304 path already avoids the read. |
| DT-062 | feature | `restore` has no parent check, so a restore racing a save lands on top of it; add optional `expectedCurrentVersionId` | P3 | this-phase | 6 | — | open | Additive to the contract. |
| DT-063 | perf | Index `versions(parent_version_id)`; cascade delete seq-scans per deleted version | P3 | later | — | — | open | Needs Flyway V2. Negligible until ~100k rows. |
| DT-064 | security | SSRF guard has no destination port restriction; Teredo server field not vetted | P3 | later | 8 | — | open | Deliberate for v0; documented in SsrfGuard KDoc. |
| DT-065 | chore | Revisit `-XX:MaxRAMPercentage=75` on the 512 MB Render starter; ~128 MB non-heap is tight | P3 | later | 8 | — | open |  |
| DT-066 | tech-debt | `CHARSET_IN_META` regex keeps a quadratic shape, safe only because it runs on the first 2048 bytes | P3 | later | — | — | open | Rewrite if the sniff window ever grows. |
| DT-067 | tech-debt | Deadline is checked between rewrite stages, not inside them; one pass over a 10 MB DOM is uninterruptible | P3 | later | — | — | open | Bounded by CLONE_MAX_BYTES today. |
| DT-068 | tech-debt | Nothing gates "a MINOR catalog release must not change rendering at default params" | P2 | this-phase | 1 | — | open | PR #6 removed the same-major byte-identical keyframes freeze (incompatible with minors adding `var(--vm-x)`). A 1.2.0 could now rewrite keyframes and pass every gate; only `fill-mode.test.ts` pins 1.0.0↔1.1.0. Idea from code-architect: gate that a minor's keyframes equal the prior version's once each NEW `var(--vm-x)` is replaced by that param's default. Saved work is safe regardless (pins isolate it). |
| DT-069 | tech-debt | `keyframesName()` validates `version` but not `animationId` (TS and Kotlin) | P2 | later | 7 | — | open | `animationId` is a bare string in `openapi.yaml`; catalog ids are pattern-constrained in `schema.json`. If the Phase 7 exporter ever names a rule before resolving the entry against the pinned catalog, a crafted id becomes injected CSS in an export. No caller today. Exporter must resolve the entry first, or the function should enforce the id pattern. From PR #6 review. |
| DT-070 | feature | Dedupe identical keyframes across pinned catalog versions | P3 | later | 7 | — | open | With full-version keyframes names (PR #6), a page mixing pins repeats identical bodies (<3 KB realistic). If it ever matters: name by the earliest version with a byte-identical body, precomputed in `manifest.json`. Names are derived, so no migration. Also revisit whether `manifest.json.keyframesNames` needs every historical version. |
| DT-071 | chore | Catalog cssVar checks assume the bare `var(--vm-x)` spelling | P3 | later | 1 | — | open | `validate.mjs` reference scan and `catalog.test.ts` both miss `var( --vm-x )` / `VAR(...)`, and the declared-but-unreferenced direction uses a literal `includes("var(--vm-x)")` so `var(--vm-x, fallback)` reads as unreferenced. Either tolerate whitespace/case/fallback in both directions (`/var\(\s*(--vm-[a-z0-9-]+)/gi`) or document "bare form only" as the contract. Also: `keyframesNameForManifest` in `gen-types.mjs` is a third copy of the name formula; `gen-types.mjs:30-32` comment overclaims; `CatalogManifestTest.kt:71` nullable Set compare. From PR #6 re-review. |
| DT-072 | feature | Durations are validated unsigned, so a negative `animation-delay` (start mid-animation) cannot be authored | P3 | this-phase | 5 | — | open | ParamValues duration regex; allow a sign for `delay` only, keep `duration` non-negative. Needs catalog min to permit it. |
| DT-073 | docs | Exporter must treat `base_html` as the trust boundary: it is served under a CSP in preview but exported with none | P2 | this-phase | 7 | — | open | Clone-time removals (comments, ping, SMIL link animation, blocked-host links) exist for this reason; re-audit the hostile golden through the exporter when it lands. |
| DT-074 | security | Render Postgres `vibe-motion-db` accepts external connections from 0.0.0.0/0 | P1 | this-phase | 8 | — | open | Blueprint default when `ipAllowList` is omitted. The api uses the internal connection string, so add `ipAllowList: []` to the database in `render.yaml` (mutable field; needs Chris's OK per CLAUDE.md rule 11). 2026-09-18: preview DBs go away with DT-075, but the PRODUCTION db is still open to the internet; still needs a decision. |
| DT-075 | chore | `previews.generation: automatic` spins up web + api + Postgres for every PR, including docs-only PRs | P2 | now | 0 | — | in-progress | Seen on first sync 2026-09-18: PR #4 and docs-only PR #8 each got a full stack (~$20/mo each while open, 7-day expiry). Suggest `manual` until Phase 8 e2e needs previews. Decision for Chris. **Decided 2026-09-18: off** (not manual); branch `chore/render-previews-off`. After merge, the existing PR #4 / PR #8 preview stacks must be deleted by hand in the Render dashboard if Render does not reap them. |
| DT-076 | test | Full-stack e2e runs locally against Docker (db + api + web), not Render previews | P1 | this-phase | 4 | — | done | Decided by Chris 2026-09-18. Today: `docker-compose.yml` has only `db`; Playwright starts `next dev` alone (Phase 3 adds MSW mocks), so no e2e touches the real API. Needs: compose `e2e` profile building `apps/api/Dockerfile` + a production web build on distinct origins, ephemeral DB, a fixture-page server, Playwright `E2E_BASE_URL` to skip `webServer`, `pnpm e2e:docker`, CI job. Blocker to design around: `SsrfGuard` only exempts loopback for tests; a fixture container gets a private (172.x) address and is refused. Subsumes DT-027. **Done in PR #11** (`pnpm e2e:docker`, `docker/e2e/README.md`). The SsrfGuard blocker was solved without touching the api: the stack network sits on a random /24 of 11.0.0.0/8. |
| DT-077 | contract | `openapi.yaml` does not enumerate `Error.code` values; the web Entry screen maps clone failures by code | P2 | this-phase | 2 | — | open | Web maps `invalid_url`, `url_blocked`, `url_unreachable`, `not_html`, `page_too_large`, `payload_too_large`, `clone_busy`, `internal_error` (read from `PageCloner.kt`/`Application.kt`), plus tolerated `login_required` / `rate_limited`. Additive fix: document the codes (enum or description) so a rename breaks typecheck instead of silently degrading copy. |
| DT-078 | feature | API: detect sign-in redirects during clone and emit `login_required` | P3 | later | 8 | — | open | The design handoff's showcased Entry error is "it redirected to a sign-in screen"; the API never emits it today, so the web sentence is only reachable through the mock. |
| DT-079 | polish | Animation picker caption says "Hover a card to preview on the page" before the bridge exists | P2 | this-phase | 4 | — | open | Copy is verbatim from the handoff; on-page preview needs the Phase 4 bridge (`apply` on hover). Becomes true when Phase 4 lands; re-check then. |
| DT-080 | tech-debt | Tuning row labels (Duration, Delay, Easing, Repeat…) live in a web-side map because catalog standard params carry no `label` | P3 | later | 1 | — | open | New catalog minor: add `label` to standard params, then delete `STANDARD_LABELS` in `components/control-panel/param-control.ts`. Sibling of DT-059. |
| DT-081 | feature | Catalog: declare easing `options` including the handoff's `spring` = `cubic-bezier(.34,1.56,.64,1)` | P3 | later | 1 | — | open | Handoff tuning panel lists a spring easing; web only offers what the catalog declares (else a fallback list). The curve preview already renders arbitrary cubic-beziers. |
| DT-082 | conflict | Handoff vs catalog: picker shows an "Exit" category chip the handoff omits; `direction`/`fillMode` rows have no mock | P3 | later | 3 | — | open | Docs win on behaviour (catalog 1.1.0 has `exit` entries and those params). `direction`/`fillMode` render as a stacked-label dense segmented; "alternate-reverse" still truncates at 320px — decided from the PR #13 screenshots ("backwa…" truncated): segmented only when ≤4 options and every label ≤8 chars, else the easing-style select, so `direction`/`fillMode` are selects. Extends DT-052. |
| DT-083 | polish | Handoff values with no token are hard-coded in web: clone-card body `#fafafa`, error line-height 1.45 | P3 | later | 3 | — | open | Ask Design for tokens or accept as literals. |
| DT-084 | security | SsrfGuard does not refuse `192.88.99.0/24`, `3fff::/20`, `100::/64`, `2001:2::/48`, `5f00::/16` | P2 | this-phase | 8 | — | open | Found by code-architect on PR #11. Reserved / deprecated ranges that slip through the deny-list. The e2e stack deliberately does NOT rely on them. |
| DT-085 | chore | GHA layer cache for the `e2e full-stack gates` job; stop building the api image twice per push | P3 | later | 0 | — | open | Cold run measured at 4m25s on PR #11; the `api` job builds the same image again. Expected ~2 min warm. |
| DT-086 | test | POST /projects rate limiter thresholds must be env-configured capacity knobs | P2 | later | 8 | — | open | `429` is in openapi but no limiter exists yet. The e2e runner is a single client address, so a fixed threshold would throttle the whole suite. Production values in render.yaml; one stack spec proves the 429 path. Distinct from the SsrfGuard no-relaxation rule. |
| DT-087 | chore | Confirm whether Render's Node runtime sets NODE_ENV=production during buildCommand | P3 | later | 8 | — | open | If it does, pnpm skips devDependencies there while `docker/e2e/web.Dockerfile` installs everything. Production builds fine today; check one Render build log and mirror it in the Dockerfile. |
| DT-088 | feature | Bridge/API: multi-origin allow-list for Render preview environments | P2 | this-phase | 4 | — | open | `WEB_ORIGIN` is a single value end to end (AppConfig, `BridgePageRenderer`, CORS, `data-vm-parent-origin`); a comma-separated list is silently mangled. Bridge spec D8 defers it. Needed before PR previews can use the editor. From PR #12 / Phase 4 plan. |
| DT-089 | tech-debt | Serve cloned pages from a dedicated sandbox origin | P1 | later | post-v0 | — | open | Cloned third-party HTML is served from the API origin with `sandbox="allow-scripts allow-same-origin"`. Safe while the API has no cookies/auth; must move to a separate origin before authentication lands. From the bridge spec architect review. From PR #12 / Phase 4 plan. |
| DT-090 | tech-debt | DT-026 second half: context-provided editor store | P2 | later | 4 | — | open | Phase 4 PR B adds `createEditorStore()` for tests and the bridge client; the React context provider replacing the module-scope default instance stays deferred. From PR #12 / Phase 4 plan. |
| DT-091 | feature | Exporter wraps animation rules in `prefers-reduced-motion: no-preference` | P2 | later | 7 | — | open | Owner decision 2026-09-18 (bridge spec §9.4): editor preview always plays, export respects reduced motion. Also: exporter must emit the `animation` shorthand (not longhands) so host longhands cannot leak in, and must hold `in-view` elements on the first keyframe (spec §6a). From PR #12 / Phase 4 plan. |
| DT-092 | bug | Selection ring can show a stale box for an element that is mid-animation | P3 | later | 4 | — | done | Fixed in PR #18: the ring marks the element's resting box at all times (offsetParent-chain layout box plus a correction recorded at rest), tracks scroll/resize/reflow even during an infinite animation; `<svg>` (no offset box) is measured live. Remaining limits are DT-141 below. |
| DT-093 | tech-debt | Decorative catalog `baseStyles` lose to more specific host rules | P3 | later | 7 | — | open | `[data-vm-id]` in preview and `.vm-aN` in export are both specificity (0,1,0), so a host rule like `.nav a` wins in both. Parity holds; the visual may not. E.g. `underline-sweep`, `shimmer`. From PR #12 / Phase 4 plan. |
| DT-094 | feature | Keyboard navigation of the animation list with live preview (↑↓, Enter) | P3 | later | 5 | — | open | In the design handoff; out of scope for Phase 4 (bridge spec §7). From PR #12 / Phase 4 plan. |
| DT-095 | bug | `IN_VIEW_THRESHOLD = 0.2` can never be reached by an element taller than 5× the viewport | P2 | later | 7 | — | open | Applies to preview and export alike. Fix idea from code-architect: thresholds `[0, 0.2]` and arm when `isIntersecting && (ratio >= 0.2 || intersectionRect.height >= 0.2 * rootBounds.height)`. From PR #12 / Phase 4 plan. |
| DT-096 | tech-debt | `ElementInfo.visible` and overlay geometry are only provable in a real browser | P3 | this-phase | 4 | — | open | jsdom has no layout. PR #12 adds a Chromium Playwright suite for the bridge; `visible` still needs an assertion there or in Task 10's e2e. From PR #12 / Phase 4 plan. |
| DT-097 | tech-debt | Host inline `animation: var(--x)` cannot be restored by the bridge | P3 | later | 4 | — | open | The shorthand with a `var()` snapshots as empty longhands, so `clear` loses it. Rare in cloned pages. From PR #12 / Phase 4 plan. |
| DT-098 | chore | `apps/web` needs `transpilePackages: ["bridge"]` when it starts importing the bridge package | P2 | this-phase | 4 | — | done | Done on `feat/4-bridge-integration` (Phase 4 Task 6): `apps/web` depends on `bridge` (`workspace:*`) and `next.config.ts` sets `transpilePackages: ["bridge"]`. |
| DT-099 | feature | Unsaved guard on element click and on Export/Restore; hoist the guard's pending-action state from `ControlPanel` to `EditorShell` | P1 | this-phase | 4 | — | open | Phase 3 guards tab switches only (Discard reverts, Keep editing stays, Save disabled). `docs/user_flow.md` §1 also guards selecting another element (needs the bridge) and Export/Restore (Phases 6–7). One dialog should serve every trigger. |
| DT-100 | feature | Wire the Save dialog and the guard's Save to `POST /projects/{id}/versions` | P1 | this-phase | 6 | — | open | `components/dialogs/save-dialog.tsx` and `lib/diff-summary.ts` are presentational/pure and shown in `/dev`; both Save buttons are disabled with a caption until Phase 6. |
| DT-101 | test | e2e for the unsaved guard | P2 | later | 4 | — | open | Not writable until the bridge can select an element in the iframe; covered by RTL tests today. |
| DT-102 | polish | `summariseDiff` names changed params by raw key (`duration 600ms → 800ms`) instead of the tuning panel's labels | P3 | later | 6 | — | open | `paramLabel` lives under `components/control-panel/`; move it to `lib/` (or into the catalog, DT-080) and reuse. |
| DT-103 | test | Bridge real-browser suite runs Chromium only | P3 | later | 4 | — | open | `packages/bridge/e2e` gates Chromium. WebKit and Firefox were probed by hand once by code-architect (replay flush, stylesheet mutation, in-view, nested hover) and nothing re-checks them per commit. From PR #12. |
| DT-104 | bug | Duplicate `data-vm-id` values in a cloned page: bridge keeps the first, base-styles rule would match both | P3 | later | 4 | — | open | Phase 2's rewriter assigns unique ids, so this only happens with hand-made fixtures or a re-cloned export. Documented in the bridge README "Known limits". From PR #12. |
| DT-105 | tech-debt | Bridge hover path reads the whole subtree `textContent` and measures twice per hover change | P3 | later | 4 | — | open | `elementInfo()` materialises full `textContent` before truncating to 80 chars, and `setHovered` calls `getBoundingClientRect` twice. Only on hovered-vmId change, not per mouse move; matters on huge containers. Walk text nodes up to the limit and reuse one rect. From PR #12 review. |
| DT-106 | chore | CI Playwright failure artifacts are empty in the `web`/`e2e` jobs | P2 | this-phase | 0 | — | open | Reporter is `github` + `list`, so `playwright-report/` is never created; traces live in `test-results/` and nothing uploads them. PR #12 fixes this for the `bridge` job only. The `web`/`e2e` jobs also download the Playwright browser on every run with no cache; PR #12 adds a version-keyed cache for the `bridge` job only. Coordinate with PR #11 (`apps/e2e`). |
| DT-107 | chore | MSW mocks have no handler for `GET /bridge/vm-bridge.js` | P2 | this-phase | 4 | — | open | **Web mock path done on `feat/4-bridge-integration`** (`apps/web/app/mock-api/bridge/vm-bridge.js/route.ts` serves the package script). Check whether an MSW handler is still wanted for unit tests, then close. Added to the contract by Phase 2. Unhandled requests bypass in the browser but error in vitest (`onUnhandledRequest: "error"`); Phase 4 needs it as soon as the web app references the bridge. |
| DT-108 | tech-debt | Web unit tests run with Node's `AbortController` installed over jsdom's | P3 | later | — | — | open | `apps/web/vitest.config.ts` execArgv preload + `vitest.setup.ts`. Needed so `fetch(..., { signal })` works under jsdom; cost: a tested component using `addEventListener(..., { signal })` throws jsdom's webidl error. Documented in the setup comment. |
| DT-109 | polish | Help page follow-ups: category blurbs live in web (move to catalog), defaults line prints raw camelCase param keys, long `cubic-bezier(...)` wraps, search matches names only | P3 | later | 1 | — | open | Blurbs and param display names want catalog metadata (siblings of DT-080). |
| DT-110 | feature | Catalog demo hints (`demoFillMode` / `demoLoop`) so exit animations do not need a web-side hold | P3 | later | 1 | — | open | `/help` clears `exit`-category demos 600ms after they end so the stage is never left empty; keyed on `category`, one named constant in `components/help/catalog-card.tsx`. |
| DT-111 | test | Small Phase 3 test/tidy items | P3 | later | 3 | — | open | `/health` mock handler untested (with DT-031); `RecentProjects` takes a test-only `now` prop; `ParamRow` label is a `<span>` (controls are named via `aria-label`); slider `getAriaLabel` closure per render; jsdom lacks `AnimationEvent` so help tests fire `webkitAnimationEnd`. |
| DT-112 | chore | Rebase note: when PR #11 (e2e → `apps/e2e`) merges, move Phase 3's `apps/web/e2e/smoke.spec.ts` edits and the `NEXT_PUBLIC_API_MOCKING` Playwright env with it | P2 | now | 3 | — | done | Whichever of #11 / #13 merges second carries the move; drop `@playwright/test` and the `e2e`/`pree2e` scripts from `apps/web/package.json` then. Done on `feat/3-web-shell-help` (61c37e7): specs moved to `apps/e2e/web/` with a new `web/mocked/` bucket. |
| DT-113 | bug | Local `web e2e` gate silently tests another worktree's dev server when port 3000 is taken | P1 | now | 0 | — | done | `apps/web/playwright.config.ts` has `reuseExistingServer: !isCI` on a fixed port. With several agents on one machine, `pnpm gates` in worktree A attaches to worktree B's `next dev` and runs A's specs against B's code: a false red (seen on PR #12: Phase 3's home page vs `main`'s assertion) or, worse, a false green. CI is unaffected. Fix: never reuse (`reuseExistingServer: false`) and pick a free port per run, or fail loudly when the port is busy. Check whether PR #11 (`apps/e2e`, per-run Docker stack) already fixes the web-only path. Update 2026-09-18: PR #11 moved the config to `apps/e2e/playwright.config.ts` but kept `reuseExistingServer: !isCI` on port 3000, so the web-only path is still exposed; it bit PR #13 too (an orphaned `next dev` without the mock API held :3000, and Next 16's per-directory dev lock blocks a second server in the same worktree). 2026-09-18 (PR #17): bit again while Phase 4 and Phase 5 Track A ran concurrently; workaround: `CI=1 pnpm e2e` sets `reuseExistingServer` false, so Playwright refuses a foreign server instead of silently testing it. Fixed in PR #20 (2026-09-19): free port for `pnpm e2e` and the gate, `reuseExistingServer` only with `VM_E2E_REUSE=1`, wiring asserted in `apps/web/package-scripts.test.ts`. |
| DT-114 | bug | Bridge: two replay/in-view edge cases play an animation one extra time | P3 | this-phase | 4 | — | open | (a) `vm-bridge.js` end-of-forced-replay guard ignores `record.preview`: a preview that ends a forced replay on an off-screen `in-view` element plays twice (spec D4 says once); fix `&& !record.preview`. (b) An `in-view` element that disarms mid-replay renders `running` for one extra off-screen play because `isArmed` is still true via `replaying`; self-heals on `animationend`, invisible. Both pre-existing at PR #12's first commit; needs a real-browser spec each. From PR #12 third re-review. |
| DT-115 | chore | Bridge PR #12 review nits | P3 | later | 4 | — | open | `rebuildRules` catch drops the refcount so a re-apply inserts then drops the rule (unreachable under the clone CSP; keep the entry and retry the insert instead), and `render.test.ts` covers only the no-previous-assignment case; `flushAnchor` all-detached fallback needs a jsdoc clause; test named "flushes on the root" asserts the opposite; CI version step does not catch a literal `undefined`; `bridge.spec.ts` third-play spec rides a fixed 250 ms wait, prefer `expect.poll`. |
| DT-116 | bug | Catalog `underline-sweep` paints a full-box gradient whenever its animation is not running (no resting `background-size` in `baseStyles`) | P1 | this-phase | 4 | — | open | `baseStyles` sets `background-image` + position but the only `background-size` is inside the keyframes, so before a `hover`/`in-view` trigger fires, or under reduced motion, the element is filled with `--vm-color`. Found via PR #13 screenshots once web started applying `baseStyles`. Needs a new catalog version adding `background-size: 0% var(--vm-thickness)` to `baseStyles` (rendering while animated is unchanged); check `shimmer` for the same class of issue. `/help` masks it for held-still demos only. The Phase 4 bridge and the exporter will hit it for real pages. |
| DT-117 | polish | Esc-to-deselect is dead once the draft is dirty, while the selected panel still says "Esc to deselect" | P2 | this-phase | 4 | — | open | Handoff: "Esc deselects (only when clean)". In Phase 3 nothing loads `currentVersionState` and Save is not wired, so after the first animation Esc never works again until Cancel. `DESELECT` cannot lose draft work, so either drop the clean-only gate or hide the hint while dirty; decide with Design when Phase 4 wires selection. From the PR #13 code review. |
| DT-118 | test | Under API mocking `MockProvider` renders nothing during SSR, so mocked e2e never exercises server-rendered HTML | P2 | later | 8 | — | open | `apps/web/mocks/MockProvider.tsx` gates the whole tree on the service worker being ready, so every route's SSR output is empty in mocked dev/e2e. The reduced-motion `/help` spec's premise ("before any JavaScript") is therefore only truly covered by the Docker stack e2e against a production build (PR #11). Either gate only routes that call the API, or add a `/help` reduced-motion spec to the stack suite. Dev/e2e only; production never mounts the provider. Update: PR #13 now runs the `/help` reduced-motion specs in the Docker stack's production build; the MockProvider SSR gate itself is unchanged. |
| DT-119 | tech-debt | Bridge client: `whenIdle()` has no timeout and `hello` has no retry | P3 | later | 4 | — | open | `apps/web/lib/bridge/client.ts`. `whenIdle()` waits forever if the frame never acks (fine for tests that ack; perf/e2e harnesses should race a deadline). `hello` is sent only when the caller asks (mount + iframe `load`); spec D7 says no retry is needed, but nothing enforces the two call sites. From Phase 4 Task 7. |

## Bugs

| ID | Type | Title | Priority | Urgency | Phase | Owner | Status | Notes |
|---|---|---|---|---|---|---|---|---|
| — | — | none yet | — | — | — | — | — | — |
| DT-120 | chore | render.yaml buildFilters omit `packages/bridge/**` (web now, api once it serves the package script) | P2 | this-phase | 4/5 | — | open | Found in PR #15: web gains a `bridge` workspace dependency, and Phase 4 Task 8 makes the api jar copy `packages/bridge/src/vm-bridge.js`. Neither service's `buildFilter.paths` lists `packages/bridge/**`, so a bridge-only change (e.g. PR A2's `BRIDGE_VERSION` 1.1.0) would not redeploy either. Add the path to both filters (mutable field). Check `feat/4-bridge-integration` first; it may already do the api half. |
| DT-121 | feature | Auto-generate result view: on-page badges for generated elements (mock 2k) | P3 | later | 5 | — | open | Plan D3 (owner call): out of Phase 5. Needs an additive bridge message + overlay rendering in `packages/bridge`. |
| DT-122 | feature | Auto-generate result view: group sibling rows (`.plan ×3`) | P3 | later | 5 | — | open | Plan D3. Needs an additive class-list/label field on `ElementInfo`. |
| DT-123 | feature | Auto-generate result view: agent summary line after the quoted prompt (mock 2k) | P3 | later | 5 | — | open | From PR #17. Needs a new `AutoResultProps` field and agent output; natural fit for the real agent (DT-003). |
| DT-124 | task | Bridge `elementInfo()` reads whole-subtree `textContent` on every hover/select of a wrapper | P3 | later | 4/5 | — | open | From PR #16. O(text) per hover change on large wrappers; `elements:query` avoids it only via the tag pre-filter. Cap the read (e.g. TreeWalker until 80 chars). |
| DT-125 | task | Bridge `elements:query`: `filter.tags` entries are not validated as strings or lower-cased | P3 | later | 5 | — | done | From PR #16. Non-string or upper-case entries silently match nothing; the shell only sends lower-case constants. |
| DT-126 | tech-debt | Control Panel re-renders the whole tuning form on every param tick (~30 ms of shell main thread at 4× CPU) | P1 | now | 4 | — | open | **Gates Phase 4 exit (code-architect, PR #18):** spec §6 stays as written (store set → ack, 16 ms at 4× throttling); the fix is ~60 lines: drop `ControlPanel`'s whole-`draftState` subscription, `React.memo` the param rows with stable callbacks, memoise the `selectDirtyVmIds` consumers; then tighten `END_TO_END_P95_MS` 64 → 16. Separate PR `fix/panel-tick-rerender` right after #18 because Phase 5 Track A edits the same components. The iframe applies a slider tick in under 1 ms (bridge round trip p95 2.1 ms, `ack.ms` max 1.0 ms), but the shell-side store-set → ack figure is p95 ~34 ms at 4× throttling because the panel re-render blocks the ack's task; 7.4 ms unthrottled. The preview is not delayed, only the shell's knowledge of it. Memoise param rows or subscribe per param, then tighten `END_TO_END_P95_MS` (64) in `apps/e2e` `bridge-perf.spec.ts`. Spec §6 words the 16 ms budget as store set → ack; the spec asserts it on the bridge path its rationale describes. Needs an owner decision on which number is the contract. From Phase 4 Tasks 9–10. |
| DT-127 | bug | Unresolvable assignments are dropped silently: `onUnresolved` is never surfaced in the panel | P2 | this-phase | 4 | — | open | Bridge spec §3: an assignment whose pinned catalog version or id cannot be resolved is not sent and the shell reports it in the panel. `createBridgeClient` reports it; `EditorShell` passes no handler and no panel affordance exists. From Phase 4 Tasks 9–10. |
| DT-128 | tech-debt | Control Panel still names elements by `data-vm-id` though the bridge reports `tag` and `textPreview` | P3 | later | 5 | — | open | `elements` side map is populated by `rememberElement`; `SelectedPanel.elementText` is unused. Handoff shows `h1` + text. From Phase 4 Tasks 9–10. |
| DT-129 | tech-debt | Mock preview silently falls back to same-origin when the dev host has no loopback sibling | P3 | later | 4 | — | open | `previewPageUrl` swaps `localhost` ↔ `127.0.0.1`; under a LAN IP or `::1` the frame is same-origin, the bridge still works but the origin check is no longer exercised. Add a dev-time warning. Also `previewOrigin()` returns an empty string during SSR, unreachable today only because `MockProvider` renders nothing until the worker starts. From Phase 4 Tasks 9–10. |
| DT-130 | tech-debt | Bridge puts no bound on a preview's lifetime; a leaked preview masks every later `apply` while acks say ok | P3 | later | 4 | — | open | Hit during Phase 4 Task 9 (a clicked card never gets `mouseleave`); fixed shell-side with an unmount cleanup in `ChoosingSection`. Belt and braces: bridge drops the preview on an `apply` to a different vmId, or the ack reports `masked: true`. Additive protocol change; coordinate with Phase 5's bridge work. From Phase 4 Tasks 9–10. |
| DT-131 | polish | Agent stagger (60 ms) is off the catalog's 50 ms `delay` step | P3 | this-phase | 5 | — | open | From PR #15 review. A generated `60ms` delay jumps to `110ms` on the first ArrowUp and the tuning slider snaps to the 50 ms grid. Save is unaffected (the API validates type/min/max, not step). Options: stagger 50 ms, or a finer catalog step in a new MINOR. Owner call; build_plan says 60. |
| DT-132 | bug | Generate on an inline link gives a hover animation that visibly does nothing | P2 | this-phase | 5 | — | open | From PR #15 review. All three `hover-*` entries are transform-only and transforms do not apply to inline boxes; `suggestForElement` has no size/display floor. Options: a `display: inline-block` baseStyle in a new catalog MINOR, or have the agent pick a non-transform animation for inline elements (needs `display` on `ElementInfo`, additive). |
| DT-133 | bug | Auto-generate double-animates nested targets (`figure > img`, `article > h2`) | P2 | later | 5 | — | open | From the PR #14 architect review and PR #15. The parent's `fillMode: both` holds `opacity: 0` over the child's entrance. De-duplicate ancestors/descendants in `selectTargets` (needs parent info on `ElementInfo`, additive) or prefer the innermost target. `picture` was dropped from `TARGET_TAGS` in PR #15. |
| DT-134 | polish | Result view's 'Skipped n elements' caption can never render in the real flow | P3 | this-phase | 5 | — | open | From PR #15 review. The bridge pre-filter (plan D4) applies the same predicate `selectTargets` re-applies, so `lastRun.skippedCount` is always 0. Track B: either drop the caption or have `elements:list` report how many tagged elements it filtered out (additive). |
| DT-135 | tech-debt | Two `ready`s per page load cause a double `state:load` that restarts `load` animations | P2 | later | 4 | — | open | Bridge posts `ready` at DOMContentLoaded and again in answer to the shell's `hello` on iframe `load`; every `ready` clears and reloads. Add a `sessionId` to `ready` so the client skips the reload when already ready for that session with nothing pending. Additive protocol change; coordinate with Phase 5's bridge work. From PR #18 architect review (N4). |
| DT-136 | tech-debt | Shell should refuse `sandbox="allow-scripts allow-same-origin"` when the preview origin equals its own outside mock mode | P2 | later | 4 | — | open | The pair is only safe while the clone is cross-origin with the shell. Assert `previewOrigin !== location.origin` when not mocking and render an error instead of the iframe; add a stack spec that both `/mock-api/...` routes 404 from the production build. Related: DT-089. From PR #18 architect review (N8). |
| DT-137 | tech-debt | Bridge client should render `selectRenderedState(state)`, re-send `mode` on `ready`, and `requestSelect` should refuse while viewing | P2 | later | 6 | — | open | Seams Phase 6 Track B needs for version viewing: the client reads `draftState` in three places (`sendStateLoad`, subscription diff, `flush`); route them through one selector. From PR #18 architect review (item 8). |
| DT-138 | tech-debt | Bridge client discards `ack.ok === false` and `unknownVmIds`; inbound `ElementInfo` is not shape-checked | P3 | later | 4 | — | open | A rejected `apply` leaves store and frame silently different; a non-string `tag` would throw in React. Add `onAckError` beside `onUnresolved` and validate before `rememberElement`. Pairs with DT-127. From PR #18 architect review (N7). |
| DT-139 | test | Flaky bridge e2e: `replay › does not play a third time when the element arms while the forced replay runs` | P2 | this-phase | 4 | — | open | Seen 2026-09-18 by test-runner on PR #15 (`cbe13ef`, a branch that does not touch `packages/bridge`): expected 2 `animationstart`, received 3 at `packages/bridge/e2e/bridge.spec.ts:188`; re-run green, CI green. Either a timing assumption in the spec or a real third play in the same family as DT-114 (arm during forced replay). Reproduce with `--repeat-each=50` before deciding; a red `bridge e2e` gate blocks every PR. |
| DT-140 | bug | screenshot-runner leaves a `next dev` server and temp Playwright specs behind in the PR worktree | P2 | this-phase | tooling | — | open | Seen on PR #17: a `next dev -p 3107` kept running after the agent finished and held Next's per-directory dev lock, so `pnpm e2e` / `pnpm gates` could not start in that worktree; seven untracked `apps/e2e/capture-*.spec.ts` / `playwright.screenshot.config.ts` files were left too. Fix in `.claude/agents/screenshot-runner.md`: write temp specs to the session scratchpad, and always stop the server it started (trap / finally), then assert `git status --porcelain` is empty before reporting. 2026-09-18, second occurrence (worse): asked to re-shoot PR #17 from its worktree on a spare port, the agent instead started `next dev` on :3000 AND the API on :8080 AND `docker compose up db` from the PRIMARY checkout (`main`), concluded the PR's frames 'do not exist', and left all three running; the stray :3000 server then failed the `web e2e` gate of PR #16's test-runner. The definition must pin the checkout to the PR worktree, forbid :3000/:8080 and the api/db for `/dev`-only shots, and verify the served page contains the requested test ids before shooting. |
| DT-141 | tech-debt | Bridge ring: known limits of the resting-box measurement | P3 | later | 4 | — | open | `packages/bridge` `layoutBox()`: an ancestor's transform changing during the play; text wrapping changing during the play (`offsetWidth` is the first fragment); keyframes that animate layout instead of transform (none in the catalog); an element selected while already mid-animation has no resting correction (exact unless an ancestor is transformed); `<svg>`/MathML rings follow the live box. Also costs one `getComputedStyle` + ~8 `offset*` + 4 `scroll*` reads per shown box per scroll frame even when nothing of ours animates (measured 0.039 → 0.047 ms mean; p95 unchanged). Documented in the bridge README. From PR #18. |
| DT-142 | bug | Bridge ring: an `in-view` element held paused on its first keyframe bakes that pose into the resting correction | P2 | this-phase | 5 | — | open | Measured a permanent 60px ring offset with an infinite animation whose first keyframe is not the identity. Catalog 1.1.0 masks it (all four infinite+transform entries start at identity); Phase 5 agent-generated or future catalog keyframes will not. Treat a held (paused) element as mid-animation for measurement purposes. From PR #18 re-review. |
| DT-143 | chore | `scripts/gates.mjs` runs the web e2e gate anyway when the free-port probe throws, and overwrites the note | P3 | later | 0 | — | open | `scripts/gates.mjs:80-85` (PR #20). Should mark the gate FAIL/SKIPPED with the probe error. Also `bridge-perf.spec.ts` replaced a frame-side handshake guard with a shell-side ack count the first handshake can satisfy. From PR #18 re-review. |
| DT-144 | chore | PR #18 leftovers: `defaultAssignmentFor` pins `CURRENT_CATALOG_VERSION` regardless of entry origin; `guardedVmId` vs `selectGuardedVmId` naming; render-cost test does not assert the panel subtree commits once; optional Gradle assertion of the `BRIDGE_VERSION` line | P3 | later | 4 | — | open | Non-blocking review items deliberately not taken in PR #18. |
| DT-145 | bug | Agent-owned assignments a later run does not re-suggest stay in the draft but are not listed in the result view | P3 | later | 5 | — | open | From Track B Task 4. Cannot happen with the mock agent (deterministic targets); can with an LLM agent (DT-003). 'Remove all' still removes them. Fix: list every agent-owned vmId, or clear unre-suggested ones on Regenerate. |
| DT-146 | polish | A failed Regenerate's error line can reappear when the user returns to the result list | P3 | later | 5 | — | open | From Track B Task 5. `useAgentRun` scopes an error to the panel state object, and the machine's `auto` state is a shared singleton, so the scope matches again until the next run. |
| DT-147 | polish | Every agent button reads 'Generating…' while any agent run is in flight | P3 | later | 5 | — | open | From Track B Task 5. `useAgentRun` is one global busy flag in `ControlPanel`; with the mock the run lasts one tick, with a real agent (DT-003) it will be visible. |
| DT-148 | polish | Tab-switch guard copy can name an untouched agent-generated element ('Save changes to h1?') | P3 | later | 5 | — | open | By design (plan Task 0 result item 8): only the element-switch guard ignores agent-owned work; generated assignments are still unsaved. Copy nuance only; revisit with Phase 6's Save flow. |
| DT-149 | tech-debt | Agent runs are not cancelled by Cancel / reset / version load; a slow agent can write into a draft that was replaced while it ran | P2 | later | 5 | — | open | From the PR #21 architect review. `lib/agent/run.ts` re-reads the store after each await and `applyPageSuggestion` re-checks ownership, but nothing ties a run to the draft it started on. Invisible with the mock (microtask latency) and with a reloaded frame (`ready` rejects the query); real with the server-side agent (DT-003). Fix: a `runEpoch` counter in the store bumped by `revertDraft`, `reset` and every Phase 6 draft replacement; `run.ts` captures it, passes an `AbortSignal` (already on the agent context) and drops the result when it moved. |
| DT-150 | tech-debt | `elements:list` reports transformed boxes; the shell works around it by preferring remembered `ElementInfo` on Regenerate | P3 | later | 5 | — | open | From the PR #21 architect review. `elementInfoFor()` uses raw `getBoundingClientRect()`, so a box measured mid-animation is the transformed one; `run.ts` prefers `store.elements` on Regenerate, which is stale after a preview resize and can itself hold a mid-animation box from `element:select`. Fix at the source: report the bridge's resting `layoutBox()` (DT-141) as `pageRect` in `elements:list`/`element:select` (no contract change), then delete the `known` preference in `run.ts`. Related: DT-133 geometric nesting wants an additive `parentVmId` on `ElementInfo`. 2026-09-19 (PR #21 fix pass): the bridge's SIZE PRE-FILTER uses the same transformed boxes, so an element held or mid-animation can drop under the 40×16 floor (or measure zero height under `flip-in-x`) and vanish from `elements:list`; on Regenerate it is then simply not re-rolled (it keeps its assignment and its row), and the 'Skipped n' count varies by seed. The stack e2e works around it by waiting for animations to finish and deriving the expected count. |
| DT-151 | test | Reshape the 'hand-animated card shields its children' stack test to pick → Save → idle → Auto-generate once Save exists | P2 | this-phase | 6 | — | open | From PR #21. Before Phase 6 a hand-picked unsaved element has no route back to idle (Esc/background click are no-ops while dirty, by design), so the test reaches the state via Auto-generate → Change → Regenerate. The fresh-page-run path is unit-tested only (`run.test.ts`). |
| DT-152 | bug | `useBridge` does not re-render when a repeated `ready` changes `bridgeVersion` without changing status | P3 | later | 5 | — | open | From PR #21 (N4). After a frame reload onto an older bridge image the Generate buttons stay enabled until the next render; the click then fails cleanly with `bridge-too-old` → 'Couldn't read the page'. Expose the version as hook state. |
| DT-153 | polish | `removeAllGenerated` reached through the store API on an empty `auto` list no longer closes to idle | P3 | later | 5 | — | open | From PR #21 (N6 identity no-op). UI-unreachable: the button is not rendered with no rows. |

Wraps a cloned-page element to draw the selection or hover ring and the mono tag.

```jsx
<SelectionRing selected tag="h1 · Fade In Up" inset={-8} radius={6}><h1>…</h1></SelectionRing>
```

Rings are pointer-events:none; attach click/hover handlers to the wrapper.

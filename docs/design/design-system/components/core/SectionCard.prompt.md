White panel card; each direct child becomes a 14px-padded section with dividers between.

```jsx
<SectionCard tabbed>
  <><SectionLabel>Trigger</SectionLabel><Segmented … /></>
  <><Slider … /><Slider … /></>
</SectionCard>
```

Also exports SectionLabel (uppercase 11px).

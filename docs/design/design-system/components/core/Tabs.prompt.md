Folder tabs that attach to the top of a tabbed SectionCard: Animate · History · Export.

```jsx
<Tabs tabs={["Animate","History","Export"]} value="Animate" />
<SectionCard tabbed>…</SectionCard>
```

disabledAll greys the inactive tabs while there are unsaved changes.

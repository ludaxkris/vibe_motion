Centered modal for the unsaved-changes guard (380) and the Save dialog (420).

```jsx
<Dialog title="Save changes to h1?" actions={<><Button variant="danger">Discard</Button><Button variant="secondary" style={{marginLeft:"auto"}}>Keep editing</Button><Button>Save</Button></>}>…</Dialog>
```

absolute positions within a mock frame instead of the viewport.

Mutually exclusive 2–4 options: trigger (load/hover/in view), repeat (1/2/3/∞), export mode.

```jsx
<Segmented options={[{value:"load",label:"On load"},{value:"hover",label:"On hover"}]} value="load" />
```

dense for the Repeat row.
